1 - Uses for assign, select assign, uses(specific, specific), input1 and input2 in select
assign a; variable v;
Select a such that Uses(a,v)
15,16,17,21,22,23,25
5000
2 - Uses for assign, select variable, uses(specific, specific), input1 and input2 in select
assign a; variable v;
Select v such that Uses(a,v)
x,y,cenX,cenY,count,normSq
5000
3 - Uses for variable, select variable, uses(specific, specific), input2 in select
variable v;
Select v such that Uses("main",v)
x,y,cenX,cenY,count,flag,normSq
5000
4 - Uses for while, select while, uses(specific, specific), input1 in select
while w;
Select w such that Uses(w,"notexist")
none
5000
5 - Uses for while, select while, uses(specific, specific), input1 in select
while w;
Select w such that Uses(w,"cenX")
14
5000






