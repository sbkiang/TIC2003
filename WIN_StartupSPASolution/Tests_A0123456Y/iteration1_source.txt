procedure main {
    wHILE = 10;
    pRINT = ((rEAD) * 2) - ((PRinT + 1) * (WHile / PRinT) / PRINT / (ReAd);
    read Read;
    print Print;
    call Call;
    if(If == ((WHilE4) + ((CaLLg)) * ((1224 - AssiGn)/(pRINT * 2212))) then{
        Call = 10;
        call CALL;
        if(iF != 20) then{
            Print = 20;
            while(WhilE < 20){
                WhilE = 30;
                WhilE = Call + Read * Print - WHile - ReAd;
            }
            read ReAd;
            print pRINTb;
        }
        else{
            pRINTb = 510;
            read ReadA;
            while(ReadA > (pRINTb * 2)){
                proceduRE = ((Procedure) / (prinT));
            }
            ReAd = pRINT2 * 2 / 2 - 2 + 2;
        }
    }
    else{
        read wHILE2;
        print wHILE2;
        if(Print < 20) then{
            Print = Print / PRINT;
            while(WhilE != 0){
                read ReAd;
                call iF;
            }
            call IF;
        }
        else{
            PRINTb = (((pRINTb))) * (((PRINTb)));
            while(cALL == (THen2 * Call + Then - WhilE / THen)){
                print theN;
            }
            call whilE;
        }
    }
}

procedure CALL{
    ProCeduRE = (ReAd)/(10);
}

procedure IF{
    PRINTb = PRINTb * pRINTb;
}

procedure procedurE{
    Call = WhilE * Then;
}